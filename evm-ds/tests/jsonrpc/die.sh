# This test runs compiled 'storage.sol', method 'store' (6057361d) with some argument: (0x3039)

curl -d '{
    "id": "1",
    "jsonrpc": "2.0",
    "method": "die",
    "params": [
    ]
}' -H "Content-Type: application/json" -X POST "http://localhost:3333"
