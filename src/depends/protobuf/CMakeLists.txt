INCLUDE(FindProtobuf)
FIND_PACKAGE(Protobuf REQUIRED)
INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIR})
PROTOBUF_GENERATE_CPP(PROTO_SRC PROTO_HEADER PoW1.proto)
ADD_LIBRARY(Proto SHARED ${PROTO_HEADER} ${PROTO_SRC})
target_include_directories (Proto PUBLIC ${PROJECT_SOURCE_DIR}/src)
target_link_libraries(Proto PUBLIC ${PROTOBUF_LIBRARY})