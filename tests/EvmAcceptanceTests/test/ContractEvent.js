const { expect } = require("chai");
const { ZilliqaHelper } = require('../helper/ZilliqaHelper');

describe("Contract Function Event generation with web3.js", function () {
    // Calling a method on a contract that generates non-anonymous non-indexed events
    // Calling a method on a contract that generates anonymous events with indexed parameters
    // Calling a method on a contract that generates non-anonymous events with indexed parameters
})